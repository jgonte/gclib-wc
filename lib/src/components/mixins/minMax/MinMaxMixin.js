const MinMaxMixin = Base => { var _a; return _a = class MinMax extends Base {
    },
    _a.properties = {
        min: {
            type: String
        },
        max: {
            type: String
        }
    },
    _a; };
export default MinMaxMixin;
