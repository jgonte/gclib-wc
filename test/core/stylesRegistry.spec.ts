import stylesRegistry from '../../src/core/helpers/stylesRegistry';

describe("stylesRegistry tests", () => {

    // stylesRegistry.loadStyle = async url => {

    //     await setTimeout(() => 'some styles', 500);
    // };

    it('should return a function', () => {

        // const type = oneOf(Object, Function);

        // const value = "getFields()";

        // const property = type.toProperty(value);

        // expect(property).toEqual((window as any).getFields);
    });
});